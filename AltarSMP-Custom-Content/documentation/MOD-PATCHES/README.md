# Optional Java-side patches (NOT applied — reference files untouched)

Each patch only ADDS Bedrock/parity capability without changing Java visual
behaviour for players who keep the current pack. Rebuild the Fabric jar after
applying any you want (`./gradlew build`). P0 is required for the Bedrock
worn-armor look on the *diamond* copper set; P1–P5 restore original-era
behaviour documented in ../DIFFERENCES.txt §3.
